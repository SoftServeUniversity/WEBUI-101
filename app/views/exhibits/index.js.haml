$('.mosaicflow').html("#{ escape_javascript render partial: 'exhibits/mosaic', locals: {exhibits: @exhibits} }")
$('.paginator').html("#{ escape_javascript(paginate(@search.hits, :remote => true).to_s) }")
