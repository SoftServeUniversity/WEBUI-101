%h1 Search Page

= form_tag('/search', method: 'get') do
  .form-inputs
    = text_field_tag(:q)
  .form-actions
    = submit_tag('Search')

%h1 Results

- if @search
  - @search.each do |query|
    - query.results.each do |result|
      %p
        = render partial: partial_for(result), locals: {result: result}
- else
  %h2 Nothing was found.
