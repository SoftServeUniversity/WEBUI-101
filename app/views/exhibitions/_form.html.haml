= simple_form_for(@exhibition) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :start_date, :as => :string, :input_html => { 'data-behaviour' => 'datepicker', 'data-date-format'=> "dd-mm-yyyy" }
    = f.input :end_date, :as => :string, :input_html => {'data-behaviour' => 'datepicker', 'data-date-format'=> "dd-mm-yyyy" }
    = f.input :adress
    = f.input :virtual

  .form-actions
    = f.button :submit
