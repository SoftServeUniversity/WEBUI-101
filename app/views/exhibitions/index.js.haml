$('.exhibitions').html("#{ escape_javascript render partial: 'exhibitions/exhibitions', locals: {exhibitions: @exhibitions} }")
$('.paginator').html("#{ escape_javascript(paginate(@search.hits, :remote => true).to_s) }")
$('.facets').html("#{ escape_javascript(render partial: 'exhibitions/facets', locals: {search: @search}) }")
